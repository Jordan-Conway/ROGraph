<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="using:ROGraph.NewUI.Views.ReadingOrderListView"
             xmlns:view="using:ROGraph.NewUI.Views.ReadingOrderListView"
             x:Class="ROGraph.NewUI.Views.ReadingOrderListView.ReadingOrderListViewControl"
             x:DataType="vm:ReadingOrderListViewModel">

    <ItemsControl ItemsSource="{Binding Overviews}">
        <ItemsControl.ItemTemplate>
            <DataTemplate>
                <Button Margin="0,10,0,0"
                        Command="{Binding NavigateCommand, RelativeSource={RelativeSource AncestorType=vm:ReadingOrderListViewControl}}">
                    <StackPanel>
                        <TextBlock Text="{Binding Name}" />
                        <TextBlock Text="{Binding Description}" />
                    </StackPanel>
                </Button>
            </DataTemplate>
        </ItemsControl.ItemTemplate>
    </ItemsControl>

</UserControl>
