<UserControl x:Class="ROGraph.Components.ReadingOrderListPageToolbar"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ROGraph.Components"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <ResourceDictionary Source="/Styles/Toolbar.xaml" />
    </UserControl.Resources>
    <DockPanel>
        <Grid Style="{StaticResource toolBarGridStyle}">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <Border Style="{StaticResource toolBarBoxStyle}" Grid.Column="0" Grid.Row="0" ToolTip="Create a New Reading Order">
                <Button>New</Button>
            </Border>
            <Border Style="{StaticResource toolBarBoxStyle}" Grid.Column="1" Grid.Row="0" ToolTip="Import an Existing Reading Order">
                <Button>Import</Button>
            </Border>
        </Grid>
    </DockPanel>
</UserControl>
